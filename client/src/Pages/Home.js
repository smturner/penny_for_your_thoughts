import React from 'react';
import LoginCard from '../components/card/loginCard'

const Home = () => {
    return (
        <div>
            <LoginCard />
        </div>
    );
};

export default Home